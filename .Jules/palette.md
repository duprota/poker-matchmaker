## 2026-04-23 - Added aria-labels to icon-only buttons
**Learning:** The project contains many icon-only buttons using `<Button size="icon">`, specifically in , that were missing accessible labels.
**Action:** Always verify icon-only buttons include descriptive `aria-label` attributes to ensure screen reader accessibility.
## 2026-04-23 - Added aria-labels to icon-only buttons
**Learning:** The project contains many icon-only buttons using \`<Button size="icon">\` that were missing accessible labels.
**Action:** Always verify icon-only buttons include descriptive \`aria-label\` attributes to ensure screen reader accessibility.
