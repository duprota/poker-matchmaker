## 2024-05-02 - Icon Buttons Accessibility
**Learning:** Many icon-only buttons (`size="icon"`) in this application are missing `aria-label`s, making them inaccessible to screen readers.
**Action:** When adding icon-only buttons or updating existing ones, always include descriptive `aria-label`s in English (even if the app text is localized).
